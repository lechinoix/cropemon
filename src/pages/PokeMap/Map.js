import React from 'react';

const containerStyle = {
  bottom: 0,
  left: 0,
  right: 0,
  position: 'absolute',
  top: 0,
}

const Map = (props) => (
  <h1>This will be the map</h1>
)

export default Map;
